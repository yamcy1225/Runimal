# RunimalDomainV2

Draft domain boundary for Runimal v2.

Owns stable, UI-independent workout recording concepts:

- recording session identity and lifecycle
- metric samples
- GPS path samples and path-rendering summaries
- completed workout archive envelope
- unassigned run resource identity

This is not yet declared in `Package.swift`; add it as a SwiftPM target in the next compile-tested phase.
